{
  "name": "hotate",
  "private": true,
  "type": "module",
  "description": "Hotate landing + hotate-cmd (intent/context → shell; nl2bash-style, macOS/Linux-aware via Ollama)",
  "scripts": {
    "dev": "vite",
    "start": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cmdgen": "node cli/bin.mjs"
  },
  "bin": {
    "hotate-cmd": "cli/bin.mjs"
  },
  "devDependencies": {
    "vite": "^5.4.21"
  }
}
